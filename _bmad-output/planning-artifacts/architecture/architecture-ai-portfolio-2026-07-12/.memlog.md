---
scope: rohanyashraj.com rebuild — gallery portfolio + Agno docent + CMS
purpose: fix stack, CMS, docent integration, hosting invariants before build
altitude: initiative→features
updated: 2026-07-12T23:15
---

- (decision) [ADOPTED] Framework: Next.js — user-declared. Binds: site rendering, routing, 404/redirect handling. Prevents: framework drift per page.
- (decision) [ADOPTED] CMS: Sanity — user-declared. Binds: single content source feeding pages AND docent knowledge (brief's irreducible requirement). Prevents: dual content stores.
- (decision) [ADOPTED] Email: Resend — user-declared. Binds: contact-form notifications + auto-acknowledgement (spec 1.4).
- (question) Open: docent runtime (Agno=Python, separate from Next.js), hosting for site+agent, message persistence, rendering strategy (SSG/ISR vs SSR)
- (version) Verified 2026-07-12: Next.js 16.2.10 LTS current stable; Agno v2.6.4 (Apr 2026, active, 39k stars); Sanity + Resend both current/active.
- (decision) Rendering: SSG/ISR — pages statically generated, revalidated via Sanity webhook. Binds: <1s paint rule, no per-request SSR. Prevents: pages that need a server to render.
- (decision) Site hosting: Vercel (free tier). Docent hosting: Railway container running FastAPI+Agno (~$5/mo). Binds: two deployables, independent lifecycles. Prevents: coupling site uptime to agent uptime (agent-off resilience is a spec invariant).
- (decision) Messages: email is the system of record. Both form and docent messages POST to one Next.js API route -> Resend (notify + auto-ack). No database. Prevents: dual inboxes, unowned message store.
- (decision) Docent knowledge: agent reads Sanity via GROQ at query time (scoped retrieval), no separate vector store at launch. Binds: zero-lag content sync, one content source. Prevents: stale duplicated knowledge base.
- (assumption) [ASSUMPTION] Budget ~$10-20/mo acceptable (Railway + LLM API); Vercel/Sanity/Resend free tiers sufficient at portfolio traffic.
- (decision) [ADOPTED] Docent LLM: gemini-3.1-flash-lite (GA model id, verified GA 2026-05-08; $0.25/$1.50 per 1M tokens). User-declared. Binds: Agno agent model config. Prevents: preview alias use (shut down 2026-05-25).
- (decision) Docent hosting: Google Cloud Run free tier (user wants free; Railway 'free' verified to be a 30-day trial that then pauses services — rejected). min-instances=0; site's health probe on page load doubles as warm-up. Binds: containerized FastAPI+Agno, stateless. Prevents: silently-paused agent; paid idle capacity.
- (decision) Monorepo: /web (Next.js) + /agent (FastAPI+Agno), two deploy targets (Vercel + Cloud Run). Binds: one repo, independent deploys. Prevents: cross-runtime imports.
- (assumption) [ASSUMPTION] Revised budget: ~$0-5/mo (all free tiers + Gemini Flash-Lite usage at portfolio traffic).
- (event) Reviewer gate run: lint clean; rubric PASS w/ findings; currency PASS (next-sanity pin needed); adversary found 6 two-owner holes
- (decision) AD-8: /api/message envelope contract — {source, name, email?, regarding?, message}; source-specific validation (form: honeypot+IP limit; docent: shared-secret header); route rejects unauthenticated docent-source posts
- (decision) AD-9: all docent-visible strings (greeting, chips, honest-miss, subtitle) live in Sanity docentSettings singleton — 'microcopy in code' defined as non-content UI labels only
- (decision) AD-10: studio/ schema is the content-shape contract — slugs immutable after publish, both web/ and agent/ read published-only perspective, schema changes are breaking changes reviewed against both consumers
- (decision) AD-11: derived stats have one owner — GROQ count queries defined once in studio/ contract, consumed identically by web/ and agent/
- (decision) AD-12: docent transport — SSE end-to-end (streaming route handler in web/ proxy, no buffering), conversation history client-owned, agent stateless per request; both public API routes rate-limited at web/ layer
- (decision) AD-2 tightened: /health is liveness-only, probed once per session on first page load, one retry after 5s; launcher renders only after success (accepts late-appear on cold start)
- (version) Stack amendment: next-sanity pinned ^13 (required for Next 16); SanityLive explicitly excluded (4x request-load hazard) — webhook->ISR only
- (direction) Deferred additions: uptime monitoring (revisit at launch — free ping service), DNS cutover runbook, Sanity draft-preview workflow
- (event) spine finalized
